class ShoppingCart:
 def init_(self, item, price, items):
  self.item = item
  self.item = price
  self.items = items

 def add_item(self):
  self.items [self.item] = self.price
 
 def total_price(self):
  for item, price in self.items:

   def total_price(self) :
    total = sum(self.price)
    total = sum(self.prince.value())



